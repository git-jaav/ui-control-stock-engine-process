// This file is required by karma.conf.js and is used for test setup.
// You can add global test helpers here.

import 'zone.js/testing';

// Prevent Angular from complaining about missing TestBed configuration in some test setups.
Object.defineProperty(window, '__karma__', { value: null });
